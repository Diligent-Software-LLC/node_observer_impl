# Changelog

## v0.1.0, 2020-05-14

### Added

- version.rb.
- Gem Specification.
- README.md.
- 'NodeObserver.instance()', 'subject(n = nil)', 'changed_node(subject = nil)', 
'subject_changed(subject = nil)', 'add(n = nil)', 'remove(n = nil)', 
'observing()', 'changed()', 'add_changed(n = nil)', 'remove_changed(n = nil)', 
'notify(n = nil)', 'observing=(s = nil)', 'changed=(s = nil)', 
'receive_change(n = nil)', 'initialize()', 
'NodeObserver.instance=(singleton = nil)', and 'NodeObserver.new()'.
- Test coverage. Some tests require integration testing.

### Edited

- Deleted Travis CI deployment settings.

### Fixed
